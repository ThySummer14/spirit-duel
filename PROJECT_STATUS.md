# 灵枢战线开发现状

更新日期：2026-08-23

## 当前结论

核心 PVE 原型已经形成完整可玩闭环：编队、自由构筑、前后场交战、幻境目标战斗、卡牌结算、AI、21 个可复用关键词、显著战斗反馈、版本化纯数据存档、本地存档和逐命令回放均已建立。当前不是商业级成品，尚未实现联机和外围养成系统。

### 架构体检（2026-08-24）

- 缓存版本自动化：`npm run cache:sync` 按内容哈希同步全部 `?v=` 参数，杜绝手动递增漏改导致的旧缓存问题。
- AI 对 AI 批量冒烟：`npm run smoke`（30 局）——零崩溃、零非法命令、零软锁；先手胜率 60/40，平均 20.8 回合，29/30 局全员满勾，阶梯升勾节奏符合预期。AI 内置僵持破局（连续空转后强制最优攻击）。
- 引擎存档校验加固：mulligansUsed / bonusUpgrades / unyielding / 勾玉等级上下限 / 生命上限均纳入反伪造校验。
- 清理旧 HUD 死样式与测试重复助手（收敛至 tests/front-helper.js）。

## 当前可玩流程

1. 在「角色编队」从 6 名原创角色中选择 4 名。
2. 进入「卡组构筑」，从每名角色的 12 种专属卡中选择 8 张，同名最多 2 张，总牌库 32 张。
3. 进入本地 PVE 对局；开局可替换手牌（最多 2 张，点击手牌即换、可点「完成调度」提前结束）。角色初始 0 勾未激活：开局最左侧首位角色免费激活（升至 1 勾），其余需逐回合升勾激活，激活后才有血条、可被效果选中、可使用其卡牌、可出击。升级阶段强制先行：回合开始必须先点选一名角色升勾（全员满勾时自动跳过），之后才能出牌或出击。升勾需齐头并进：所有角色达到相同勾玉等级后，才能把任何角色升向下一级；先手第 7 回合获得一次额外升勾机会。
4. 桌面端可把准备区角色拖到己方战斗区出击，也可直接拖到敌方战斗区角色或幻境上指定目标；需要选目标的手牌（法术、治疗、战斗牌）可拖到目标角色身上直接施放；触屏端保留点选流程。
5. 战斗区初始为空：角色出击后进入战斗区并留场穿越对手回合，对方战斗区有人时互相交战、空缺时直击核心；气绝立即离场且不自动补位，己方回合开始时战斗区角色自动归位准备区。核心归零或空牌库再次抽牌时结束对局。
6. 顶部「战报」按钮打开独立滚动面板，记录数量不会拉长战场。
7. 构筑页和战场会展示角色被动、关键词、攻击、生命、勾玉、护盾、眩晕、晶裂、气绝和关键词资源。
8. 通过代表牌可体验响应、瞬发、贯通、眩晕、远程、运势、鼓舞、充能、倒计时、占卜、化身、融合、协战、投射，以及连击、先攻、暴击、不屈——四条新关键词在银狼与金刚的卡牌上有完整展示。
9. 响应触发覆盖治疗/回复核心、复活、冻结三类新场景（断流、摄魂税、融雪暖光），叠加原有伤害/出击/护盾触发，支持 LIFO 连锁与嵌套。
9. 顶部「存档」可保存或读取单个本地对局槽；刷新页面后存档仍保留。
10. 可从「存档」回放当前对局或本地存档，通过时间轴、滑杆和播放控制逐步检查每条命令后的战场状态。
11. 可把当前对局或本地存档导出为 JSON，并导入他人的合法文件直接进入只读回放。
12. 长对局回放不再预生成全部状态帧，改为每 20 条命令建立检查点，并将近期完整帧缓存限制为 12 个。

## 已完成

### 内容与构筑

- 8 名原创角色、114 种卡牌：原 6 名各 15 张候选，新角色「银狼」（连击/先攻游击手，被动刃胄：交战后获 1 点护盾）与「金刚」（不屈铁壁，被动石肤：前线回合开始自愈 1 点）各 12 张候选，原角色 8 张默认构筑、新角色默认构筑同规格。
- 卡牌包含费用、勾玉、类型、稀有度、战术标签、关键词和只读的“条件 + 动作 + 目标”效果步骤。
- 自定义 4 人、每人 8 张的牌组构筑；总数、归属和同名上限由规则层校验。
- 默认构筑与玩家锁定构筑分离，开始对局和再战均使用玩家牌组。
- 内容目录会拒绝未知条件、动作、目标、关键词和无效关键词配置。

### 核心战斗

- 30 点核心生命、2 点鬼火、抽牌、牌库枯竭、出击、反击、免费升勾、气绝与复归。
- 战斗牌、法术牌、形态牌、幻境牌及通用目标选择。
- 真实 `frontUnitId` 战斗区状态（本家规则）：开局为空，出击进入、替换出击、气绝离场不补位、回合开始自动归位。
- 6 个纯数据被动：余烬追击、岩壁、月返、霜痕、追风、墨护。
- FIFO 被动事件队列、64 次事件链上限、结束时队列清理和纯 JSON 对局状态。
- 纯数据 LIFO 结算栈和连锁响应窗口；双方交替持有优先权，连续放弃两次才结算当前栈顶，响应最多嵌套 8 层。
- AI 位于独立的纯评分模块，通过命令对象执行出牌、升勾、出击、响应/放弃响应、占卜选择和结束回合。
- 幻境有唯一运行时 `uid`、当前/最大耐久、受击与摧毁事件；基础出击和战斗牌都可指定攻击幻境，不触发反击。
- `GAME_STATE_VERSION = 12`（v12：手牌上限焚毁规则），`serializeGame` / `deserializeGame` 额外验证幻境来源、唯一身份、耐久、结算帧、响应优先权与栈深。

### 14 个关键词

- **响应**：`霜障` 可响应伤害、出击或护盾；支持双方连续响应、优先权转移、两次连续放弃和 8 层嵌套上限。
- **瞬发**：`月幕` 作为己方回合第一张瞬发牌时费用为 0。
- **贯通**：`熔锋决` 将角色的真实溢出伤害转移给敌方核心。
- **眩晕**：`静默霜域` 与 `寒束` 阻止角色出击和反击；玩家文案统一为“眩晕”。
- **远程**：`疾电` 从当前位置攻击，不换入前线且不受反击。
- **运势**：`曙针` 结算前使用确定性 RNG 投 D6，达到 4 时追加 2 点伤害。
- **鼓舞**：`辉月鼓舞` 为下一次实际出击提供可叠加攻击和护盾。
- **充能**：霆鸢每个己方回合获得充能，`聚雷矢` 支付 2 点充能后使用。
- **倒计时**：`焚线` 在持有者回合开始时推进，归零触发后重置。
- **占卜**：`索引页` 暂停结算并检视牌库顶 3 张，玩家或 AI 必须选择一张置顶。
- **化身**：`残影回环` 在己方回合抽牌后可自动免费使用，每回合最多一次。
- **融合**：`云隙之相` 重复使用时为来源角色叠层强化，达到上限后不可再用。
- **协战**：`逐光` 在本回合已有另一名角色攻击后获得攻击加成。
- **投射**：`墨渍` 优先命中敌方前线，前线为空时改为伤害核心。

关键词统一由 `game-keywords.js` 注册。费用、合法性、回合开始、结算前支付、效果条件、伤害路由、交战参数、交战后记录、自动出牌、幻境触发、UI 状态和存档校验均使用生命周期钩子。核心层不识别代表卡或角色 ID。

具体配置契约和新增角色复用流程见 [`KEYWORD_BLUEPRINT.md`](KEYWORD_BLUEPRINT.md)。

### 存档与确定性

- 运势使用对局确定性 RNG，AI 模拟和真实结算使用同一规则。
- 占卜的候选、玩家、结算帧引用和暂停状态可序列化恢复。
- 恢复时验证占卜候选仍在对应牌库且结算帧存在。
- 充能按角色 UID 保存并验证上限；融合按角色 UID 和卡牌 ID 保存层数与累计数值。
- 损坏的关键词状态、伪造的融合数值和无效占卜状态会被拒绝。
- 运行时事件队列和执行标记不会进入持久存档。
- `game-session.js` 记录玩家和 AI 的 `play-card`、`level-up`、`attack`、`end-turn`、`pass-response` 与 `divination-choice` 命令。
- 本地会话存档同时包含初始 v6 快照、当前 v6 状态和连续命令日志；保存与读取都会完整重放并拒绝状态不一致、非法命令或损坏序号。
- 单存档槽使用浏览器 `localStorage`，弹层显示当前回合、命令数和存档时间；读取后同步恢复编成、牌组、前线、手牌、AI 回合与待处理规则状态。
- `createCommandReplay()` 初始化时只保留初始帧和已校验终局，访问中间步骤时从最近的缓存帧或每 20 条命令检查点继续确定性重放。
- 近期完整帧使用上限为 12 的 LRU 缓存；检查点独立保留。`createCommandReplayFrames()` 仅作为需要完整帧数组的兼容 API，不再由界面调用。

### 命令时间轴与回放

- 当前对局和本地存档各有独立回放入口；无命令的当前对局不会提供空回放。
- 回放控制支持开头、上一步、自动播放/暂停、下一步、结尾、滑杆和可点击命令时间轴。
- 顶部显示回放来源、当前步骤、总步骤和命令摘要；战场、手牌、战报与资源全部读取对应序列化帧。
- 回放期间禁用编成、存档、重开、出牌、升勾、出击和结束回合，并暂停 AI、占卜弹层与胜负弹层。
- 只有单步前进播放正向战斗反馈；后退和任意跳转不会伪造反向受击动画。
- 退出回放直接恢复未被修改的实时对局，不将回放帧写回 `game`，也不追加命令日志。
- 导出复用完整的 `SESSION_SAVE_VERSION = 1` 信封，保留保存时间、最终状态、初始快照和双方命令日志。
- 导入文件限制为 5 MB，并复用 `restoreSessionSave()` 校验版本、对局结构、命令序号和重放最终状态；导入只进入 `IMPORTED FILE` 回放，不写入实时对局、当前命令日志或 `localStorage`。

### 界面与交互

- 战场自上而下为「敌方准备区、敌方战斗区、指令区、己方战斗区、己方准备区」，双方战斗区紧贴中央交战轴；手牌拥有独立固定区域。
- 头像角落化：敌方头像居左上、我方居左下，血量徽章贴头像右下角，旁边是鬼火火焰与出击次数标记；幻境收缩为头像下方的小方块，点击弹出效果预览（仍可作为出击/卡牌目标拖拽）。右缘为上下两摞待抽牌堆（点击提示剩余张数），中间是结束回合按钮。
- 拖动角色或卡牌时悬停检视层自动隐藏，不遮挡视线；基础出击（每回合一次）与战斗牌（耗鬼火的额外攻击）分开结算，互不占用。
- 气绝复活（倒计时归队与卡牌复活）一律回复全部生命。
- 对局界面应用 v2 视觉皮肤：舞台极光晕与暗角、玻璃药丸状态条、三色指令按钮（金/朱/玉）、手牌金光呼吸与悬停抬升、单位卡内衬发丝线、统一玻璃对话壳。
- 战斗 BGM 双曲目可选：阴郁·平调子（原曲）与热血·冲锋（快节奏太鼓驱动 + 高亢尺八），声音设置内切换、即时生效并持久化；手牌悬停大卡预览上浮至头像盘之上不再被遮挡。
- 失序体随机编成：每局从全部 8 名角色中随机抽 4 名组成敌方阵容（开战时播报），PVE 对局不再固定阵容。
- 对齐百闻牌 UI 报告：手牌按式神分组、卡牌类型顶部轮廓（战斗尖角/形态圆润/法术多边形）、瞬发牌标识、结束回合无可行操作自动亮起、先手标识、响应牌窗口期脉冲、伤害数字随数值缩放、可出击角色脉动光效、出牌卡面展示（己方左侧/敌方右侧）、战斗区椭圆形高亮、手牌上限 12 张超出焚毁。
- 攻击与生命数值使用高对比徽标，战斗区承担者和可出击角色无需依赖战报判断。
- 悬停检视层为「式神録」卷轴式档案：头像圆徽 + 称号 + 大字属性栏（攻击/生命/勾玉）、引言式被动、彩色状态签与关键词注记；按区域决定展开方向（敌方朝下、我方朝上），悬停时整卡抬升不被邻卡遮挡。
- 战报为独立固定高度滚动弹层，长记录不改变战场高度。
- 攻击前冲、远程脉冲、受击震动、伤害数字、升勾爆发、气绝退场、复归和核心受击反馈已接入。
- AI 会等待关键反馈播放完成；战报、规则和占卜弹层打开时暂停后续动作。
- 占卜弹层不可关闭，桌面显示三列候选，390px 移动端显示单列候选。
- 手牌等待占卜时显示 `choice-wait`；融合达到上限显示 `fusion-max`。
- 角色卡显示充能与融合状态，行动区显示鼓舞与本回合协战攻击者数量。
- 幻境席显示当前/最大耐久和耐久条；可攻击目标、受击跳字、震动与摧毁中央播报已接入。
- 减少动态效果模式提供高对比静态反馈，键盘焦点和弹层语义已纳入审计。

## 尚未完成

- 旧版本会话存档与回放文件的迁移策略。
- 可撤回的行动预演；当前只能取消尚未结算的选择。
- AI 难度档位、性格权重和多步搜索；当前为一步模拟与可配置评分。
- 联机同步、服务端裁决、匹配、收藏、商店、天梯、剧情和社交系统。
- 商业级数值平衡、完整美术与音效、动画细节打磨和无障碍专项测试。

## 下一阶段 To-do

1. 为 AI 增加难度与性格权重，并扩充固定残局测试。
2. 扩充角色前先建立费用、身材、伤害和组合收益的数值预算表。

## 验收基线

- `npm test`：90 项测试通过，覆盖连锁响应、AI 响应决策、幻境目标、v6 存档校验、按需回放检查点、有限帧缓存、双方命令重放、会话存档往返与损坏日志拒绝。
- `npm run audit`：0 错误、0 警告；原创素材、语义结构、响应式、键盘焦点和动效降级检查通过。
- 语法检查：`game-keywords.js`、`game-core.js`、`game-content.js`、`game-ai.js`、`game-presentation.js` 和 `app.js` 均通过 `node --check`。
- 桌面占卜：1440×1000 下弹层显示 3 个候选、不可关闭，文档宽度等于视口宽度，控制台 0 错误、0 警告。
- 移动占卜：390×844 下候选为单列，弹层宽 352px，文档宽度与视口同为 390px。
- 占卜结算：选择“蚀字”后弹层关闭、手牌解除等待状态，战报记录“开始占卜 → 将蚀字置顶 → 使用索引页”。
- 充能、鼓舞、远程、贯通、响应、倒计时和长战报均已有真实浏览器回归；其他新增关键词由固定规则、AI 和存档测试覆盖。
- 幻境目标已在 1440×1000 和 390×844 实浏：点击后只扣除幻境耐久，敌方前线不受伤，攻击者不受反击；两种视口文档宽度均等于视口宽度，控制台 0 错误、0 警告。
- 本地存档已实浏“升勾后保存 → 出击改变双方生命与鬼火 → 读取恢复”，并验证刷新后仍可读取；390×844 下文档宽度与视口同为 390px，弹层宽 352px 且完整位于视口内，控制台 0 错误、0 警告。
- 命令回放已实浏当前对局和本地存档两种来源；验证初始、升勾、出击三帧，可直接跳转和自动播放，退出后实时对局状态与命令数不变。
- 回放移动端在 390×844 下文档宽度与视口同为 390px，控制条完整、时间轴可横向滚动；控制台 0 错误、0 警告。
- 回放文件已实浏“导出 1 条命令 → 现场继续至 2 条命令 → 导入旧文件 → 退出恢复 2 条命令现场”；版本 999 的损坏文件被拒绝，且不会覆盖当前对局。
- 导入/导出弹层在 390×844 下宽 358px、无横向溢出；当前与存档导出文件分别验证包含 1 条和 2 条连续命令，控制台 0 错误、0 警告。
- 连锁响应已实浏“玩家伤害 → AI 响应 → 玩家放弃 → AI 再响应”，并验证从玩家回合中的 AI 响应权存档恢复后会自动继续；界面正确显示优先权、深度 2/8 和连续放弃 0/2。
- 连锁响应在 390×844 下页面宽度与视口同为 390px，响应提示和“放弃响应”按钮无溢出，控制台 0 错误、0 警告。
- 本轮截图位于 `output/playwright/realm-target-desktop.png`、`realm-hit-desktop-final.png`、`realm-destroyed-desktop.png` 和 `realm-target-mobile.png`。
- 本轮桌面与移动端占卜截图位于 `output/playwright/keywords14-divination-desktop.png` 和 `output/playwright/keywords14-divination-mobile.png`。
- 本地存档截图位于 `output/playwright/session-save-desktop.png`、`session-restored-desktop.png` 和 `session-save-mobile.png`。
- 回放截图位于 `output/playwright/replay-timeline-desktop.png`、`replay-final-desktop.png` 和 `replay-timeline-mobile.png`。
- 回放文件弹层截图位于 `output/playwright/replay-file-dialog-desktop.png` 和 `replay-file-dialog-mobile.png`。
- 连锁响应移动端截图位于 `output/playwright/response-chain-mobile.png`。

## 文件责任

| 文件 | 责任 |
| --- | --- |
| `game-content.js` | 角色、卡牌、规则常量和内容契约 |
| `game-keywords.js` | 关键词注册表、生命周期、状态格式化与校验 |
| `game-core.js` | 战斗状态、结算栈、事件队列、命令、存档与规则 facade |
| `game-ai.js` | 纯 AI 命令评分和选择 |
| `game-presentation.js` | 从状态快照推导视觉反馈 |
| `game-session.js` | 双方命令日志、检查点、有限按需帧缓存、确定性重放与本地会话存档校验 |
| `app.js` | 构筑、渲染、交互编排、AI 执行、存档入口、JSON 文件交换和只读回放播放器 |
| `styles.css` / `formation.css` | 通用视觉 token、战场与响应式布局 |
